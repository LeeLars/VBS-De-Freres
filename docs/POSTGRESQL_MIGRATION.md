# PostgreSQL Migrations

In `/cms/database/migrations` kunnen SQL- of JS-gebaseerde migrations geplaatst worden.

- `001_init.sql` – basis tabellen (bijvoorbeeld pages, services, pricing, ...).
- Voeg per wijziging een nieuw bestand toe met oplopend nummer.

De starter bevat nog geen echte migrations; voeg deze per project toe.
